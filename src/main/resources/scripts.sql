1. select *
   from student
   where age >13 and age <15

2.select "name"
  from student

3. select "name"
   from student
   where name like '%o%'

4. select "name"
   from student
   where age < id

5.    select "name", "age"
      from student
      order by age